{
  "annotations": {"list": []},
  "editable": true,
  "gnetId": null,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {"type": "row", "title": "Sistema", "gridPos": {"h": 1, "w": 24, "x": 0, "y": 0}},
    {
      "type": "timeseries",
      "title": "JVM Memory Used",
      "datasource": "Prometheus",
  "targets": [{"expr": "sum(jvm_memory_used_bytes)", "format": "time_series"}],
      "gridPos": {"h": 6, "w": 12, "x": 0, "y": 1}
    },
    {
      "type": "timeseries",
      "title": "GC Pause Time (ms)",
      "datasource": "Prometheus",
  "targets": [{"expr": "sum(rate(jvm_gc_pause_seconds_sum[5m]))", "format": "time_series"}],
      "gridPos": {"h": 6, "w": 12, "x": 12, "y": 1}
    },
    {
      "type": "timeseries",
      "title": "HTTP Request Latency (p95)",
      "datasource": "Prometheus",
  "targets": [{"expr": "histogram_quantile(0.95, sum(rate(http_server_requests_seconds_bucket[5m])) by (le))", "format": "time_series"}],
      "gridPos": {"h": 6, "w": 12, "x": 0, "y": 8}
    },
    {
      "type": "timeseries",
      "title": "HTTP Error Rate (5m)",
      "datasource": "Prometheus",
  "targets": [{"expr": "sum(rate(http_server_requests_seconds_count{status=~\"5..\"}[5m])) / sum(rate(http_server_requests_seconds_count[5m]))", "format": "time_series"}],
      "gridPos": {"h": 6, "w": 12, "x": 12, "y": 8}
    }
  ],
  "schemaVersion": 31,
  "style": "dark",
  "tags": ["system","jvm","http"],
  "templating": {"list": []},
  "time": {"from": "now-1h", "to": "now"},
  "timezone": "browser",
  "title": "Delivery API - Sistema",
  "uid": "delivery-system",
  "version": 1
}
